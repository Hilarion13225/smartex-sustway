fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.13.4
pydantic-settings==2.7.0
httpx==0.28.1
sqlalchemy==2.0.36
psycopg[binary]==3.2.3
redis==5.2.1
boto3==1.35.90

# Agents IA (Document/Evidence/Compliance) — palier gratuit Google AI Studio.
# Épingle < 3.0.0 sur recommandation officielle du SDK (changement de
# comportement prévu en v3 sur l'appel de fonctions automatique — sans
# impact ici, mais épinglé par prudence).
google-genai>=2.18.0,<3.0.0

# Extraction de référentiels importés (Phase 3D). Ces trois bibliothèques
# servent à MESURER la localisation — page, feuille et ligne, paragraphe —
# plutôt qu'à la demander au modèle, qui répondrait quelque chose de
# plausible sans être vérifiable. Elles permettent aussi de découper un
# document volumineux sur des frontières connues avant de l'envoyer au
# modèle, ce qu'un envoi multimodal direct ne permettrait pas.
pypdf==5.1.0
python-docx==1.1.2
openpyxl==3.1.5

# Vérification des jetons de service émis par l'API (RS256). Seule la clé
# publique est nécessaire ici : la clé privée ne quitte jamais l'API.
pyjwt[crypto]==2.10.1
