package com.smartexsustway.api.domain.enums;

/** Correspond au type PostgreSQL {@code statut_execution_agent} : issue d'un appel au fournisseur. */
public enum StatutExecutionAgent {
    EN_ATTENTE,
    EN_COURS,
    TERMINE,
    ERREUR
}
